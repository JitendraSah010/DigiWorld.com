<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <title>index page</title>
    <link rel="stylesheet" href="/../css/main.css">
    <link rel="stylesheet" href="/../css/style.css">
</head>
<body>
  <%- include('adminNav')%>

    <section style="margin-top: 100px;">
      <% if(EditProduct.length !=0) {%>
        <div class="container mt-5">
            <div class="row justify-content-center">
                <div class=" col-md-8">
                    <h2 class="fw-bold">All Products</h2>
                    <p class="mb-4">Following are the products you have added.</p>
                    <table class="table">
                        <thead>
                          <tr>
                            <th scope="col">S.N</th>
                            <th scope="col">Name</th>
                            <th scope="col">Price</th>
                            <th scope="col">Action</th>
                          </tr>
                        </thead>
                        <tbody>
                            <%for(let i=0; i< EditProduct.length; i++) {%>
                          <tr>
                            <td> <%=i+1 %> </td>
                            <td> <%= EditProduct[i].title%> </td>
                            <td> <%= EditProduct[i].pricing %> </td>
                            <td>
                                <form action="/admin/update-product" method="post">
                                    <input type="hidden" name="id" value="<%= EditProduct[i]._id %>" >
                                    <button class="callback-update-btn">Edit</button>
                                </form>
                                <form action="/delete-product" method="post">
                                  <input type="hidden" name="id" value="<%= EditProduct[i]._id %>" >
                                  <button class="callback-update-btn text-danger">Delete</button>
                                </form>
                            </td>
                          </tr>
                        <%}%>
                        </tbody>
                      </table>
                </div>
            </div>
        </div>
        <%} else {%>
            <h3 class="text-center mt-5 py-3 fw-bold text-danger noCallback">No Callback Requests</h3>
          <%}%>
    </section>
</body>
</html>