<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DigiWorld.com/login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class=" col-md-5">
                <% if(message.error) {%>
                    <p class=" fw-bold regMag text-center py-2 px-1 mt-3 mb-1" id="loginError" > <%= message.error%> </p>
                <%}%>
                <% if(message.success) {%>
                    <p class=" fw-bold regMag suces text-center text-success py-2 px-1 mt-3 mb-1" id="loginError" > <%= message.success%> </p>
                <%}%>
                <div class="card mt-4">
                    <div class="card-body">
                        <div class="text-center">
                            <!-- <h2 class="login-heading fw-bold">Manage Todo</h2> -->
                            <img src="loginn.png" alt="" class="login-img">
                            <h2 class="login-heading fw-bold">Login</h2>
                            <p class="para mt-4">Please login your account at <span class="fw-bold text-black">Digi<span class="world">World</span></span> </p>
                            
                            <form action="/auth" method="post" class="showPasDiv">
                                <div class="form-floating mb-3">
                                    <input type="email" name="email" class="form-control login-input" id="floatingInput" placeholder="name@example.com">
                                    <label for="floatingInput">Email Address</label>
                                  </div>
                                <div class="form-floating mb-3 ">
                                    <input type="password" name="password" class="form-control login-input" id="password" placeholder="name@example.com">
                                    <label for="floatingInput">Password</label>
                                </div>
                                <button class="btn btn-dark px-3 my-3 fw-bold py-1 fs-6 login-btn mt-4 " type="submit"> Login </button>                       
                            </form>
                            <img src="show.png" class="showPassword d-none" id="hide" onclick="hide()">
                            <img src="hide.png" class="showPassword" id="show" onclick="show()">
                            <a href="/forgot-password" class="forgetPassword">forgot password</a>
                        </div>
                        <p class="text-center font" >Don't have account?<a href="/register" class="ms-1">Register Now</a> </p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        let hideImage = document.getElementById("hide");
        let showImage = document.getElementById("show");
        function show(){     
            document.getElementById("password").type = "text";
            showImage.style.display = "none";
            hideImage.classList.remove("d-none");
        }
        function hide(){      
            document.getElementById("password").type = "password";
            hideImage.classList.add("d-none");
            showImage.style.display = "block";
        }
        // to hide alert after 5 second
        setTimeout( ()=> {
            document.getElementById("loginError").classList.add("d-none");
        }, 4000);
    </script>

</body>

</html>